(ns core)

;; user=> (load-file "yourfile.clj")

(load-file "module1.clj")
(in-ns 'module1)


(defn coreMain []
  
  (println "Start Git Repo")
  (println "Excute Core Main ! !")

  )

(coreMain)
(moudleFun)
